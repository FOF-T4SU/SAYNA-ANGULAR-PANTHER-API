const db = require("./data/db.json");
const produits = require("./data/produits.json");
const utilisateursInscrits = require("./data/utilisateursInscrits.json");

module.exports = {
    db,
    produits,
    utilisateursInscrits,
};